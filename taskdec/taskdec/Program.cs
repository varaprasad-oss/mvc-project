using System;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

namespace taskdec
{
    class Program
    {
        static void Main(string[] args)
        {
            //Class9 obj = new Class9();
            //obj.display();

            //stri

            Class1 obj = new Class1();
            obj.GetDirector();
            //obj.Dis();
            //obj.Fil();
            //obj.GetFiles();

            //Class2 on = new Class2();
            //on.avarage();
            //new Class3().StringIndex();
            //new Class4().sf();
            ////Class6.ps();
            ///p
            //string[] filePaths = Directory.GetDirectories(@"C:\");
            //Parallel.For(0, filePaths.Length, (index) =>
            //  {
            //      Console.WriteLine(filePaths[index]);
            //  });
            //var li = new List<string>();
            //for (int i = 0; i < 10; i++)
            //{
            //    Console.WriteLine($"enter the string at {i}");
            //    li.Add(Console.ReadLine());
            //}
            //Parallel.ForEach(li, (list) =>
            //     {
            //         Console.WriteLine(list);
            //     });
            //var numbers = new List<int>();
            //for (int i = 0; i < 5; i++)
            //{
            //    Console.WriteLine($"enter the numbers at{i}");
            //    numbers.Add(int.Parse(Console.ReadLine()));
            //}
            //Parallel.For(0, numbers.Count, (item) =>
            //{

            //    Console.WriteLine(numbers[item]);
            //});
            //Parallel.ForEach(numbers, (index) =>
            //{
            //    Console.WriteLine("using the parallel foreach loop");
            //    Console.WriteLine(index);

            //});


        }
    }
}
